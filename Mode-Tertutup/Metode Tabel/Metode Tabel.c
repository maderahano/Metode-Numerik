#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#define MAX 255

float fungsi_eksponen(float );
float fungsi_h(float ,float , float);
float fungsi_xi(float ,float ,float );
void tabel(float , float , float);

int main()
{
	float a, b, h, xi[MAX];
	int n, i;
	
	printf("Fungsi = e^x + x\n\n");
	printf("Input Batas (a,b) = ");
	scanf("%f,%f", &a, &b);
	printf("Input Jumlah Percobaan = ");
	scanf("%i", %n);
	
	tabel(float a, float b, float n)
	for(i=0)
} 

float fungsi_h(float a, float b, float n)
{
	float hasil;
	
	hasil = b - a/n;
	return hasil;
}

float fungsi_xi(float a, float i, float h)
{
	float hasil;
	
	hasil = a + i*h;
	return hasil;
}

float fungsi_eksponen(float x)
{
	float hasil;
	
	hasil = exp(x) + x;
	return hasil;
}

void tabel(float a, float b, float n)
{
	
}
